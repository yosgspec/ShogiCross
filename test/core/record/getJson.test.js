import {Record} from "@/core/record.js";

describe("Record.getJson", ()=>{
    test("should return the record data as JSON string", ()=>{
        // テストコードをここに記述
        // 例:
        // const board = {getPiecesText: ()=> "", field: [], onTurnEnd: ()=>{}}; // モックのboardオブジェクト
        // const record = new Record(board);
        // const json = record.getJson();
        // expect(typeof json).toBe("string");
        // expect(json).toContain("records");
   });
});
