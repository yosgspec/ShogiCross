import {Record} from "@/core/record.js";

describe("Record.getComment", ()=>{
    test("should return the comment for the specified turn", ()=>{
        // テストコードをここに記述
        // 例:
        // const board = {}; // モックのboardオブジェクト
        // const record = new Record(board);
        // record.add({inc: 1, end: "テスト"});
        // record.addComment("これはコメントです");
        // const comment = record.getComment();
        // expect(comment).toBe("これはコメントです");
   });
});
