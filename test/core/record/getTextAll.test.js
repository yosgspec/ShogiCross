import {Record} from "@/core/record.js";

describe("Record.getTextAll", ()=>{
    test("should return all record texts", ()=>{
        // テストコードをここに記述
        // 例:
        // const board = {xLen: 9, getPiecesText: ()=> "", onTurnEnd: ()=>{}}; // モックのboardオブジェクト
        // const record = new Record(board);
        // record.add({inc: 1, end: "テスト1", fromPanel: {pX: 0, pY: 0}, toPanel: {pX: 1, pY: 1}, piece: {deg: 0, char: "歩"}});
        // record.add({inc: 1, end: "テスト2", fromPanel: {pX: 1, pY: 1}, toPanel: {pX: 2, pY: 2}, piece: {deg: 0, char: "歩"}});
        // const textAll = record.getTextAll();
        // expect(textAll).toContain("1: ");
        // expect(textAll).toContain("2: ");
   });
});
