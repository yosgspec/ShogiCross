import {Record} from "@/core/record.js";

describe("Record.setJson", ()=>{
    test("should set the record data from JSON string", ()=>{
        // テストコードをここに記述
        // 例:
        // const board = {setPiecesText: vi.fn(), field: [], onTurnEnd: ()=>{}}; // モックのboardオブジェクト
        // const record = new Record(board);
        // const jsonString = JSON.stringify([{inc: 0, end: "開始局面"}, {inc: 1, end: "テスト"}]);
        // record.setJson(encodeURI(jsonString), 1);
        // expect(record.turn).toBe(1);
        // expect(record.records.length).toBe(2);
   });
});
