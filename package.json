{
  "name": "shogicross",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "run-s build:*",
    "build:vite": "npx vite build",
    "build:after": "node buildAfter.js ./dist/ShogiCross",
    "build:dts": "npx tsc",
    "build:sharp": "npx sharp -i ./src/img/**/*.png -f png -o {dir} ",
    "build:api": "npx documentation build ./src/ShogiCross/lib.js -f html -o ./src/doc/api"
  },
  "dependencies": {
  },
  "devDependencies": {
    "adm-zip": "^0.5.12",
    "npm-run-all": "^4.1.5",
    "sharp-cli": "^4.2.0",
    "typescript": "^5.4.2",
    "vite": "^5.1.5"
  }
}
