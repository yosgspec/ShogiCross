/** 駒の移動判定
 * @param {Board} board - ボード
 * @param {Piece} piece - 駒
 * @param {number} pX - マス目の列
 * @param {number} pY - マス目の行
 * @returns
 */
export function checkTarget(board: Board, piece: Piece, pX: number, pY: number): void;
import { Board } from "./board.js";
import { Piece } from "./piece.js";
